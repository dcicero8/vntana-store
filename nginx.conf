server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # clean paths: /imts serves imts.html (both /imts and /imts.html work)
        try_files $uri $uri.html $uri/ /index.html;
    }
}
